---
const navItems = [
  {
    label: "invest",
    url: "/#invest",
  },
  {
    label: "avi",
    url: "/#avi",
  },
  {
    label: "the foundation",
    url: "/#the-foundation",
  },
  {
    label: "contact",
    url: "/#contact",
  },
];
---

<header
  class="sticky top-0 z-10 flex min-h-(--header-height) w-full justify-center"
>
  <nav class="flex w-full max-w-6xl items-center justify-between px-6">
    <a>
      <img src="/logo.png" alt="ig9tor logo" class="h-8" />
    </a>
    <div class="flex items-center gap-4">
      {
        navItems.map((link) => (
          <a
            class="relative block px-2 py-2 transition hover:text-blue-500 dark:hover:text-blue-500"
            aria-label={link.label}
            href={link.url}
          >
            {link.label}
          </a>
        ))
      }
    </div>
  </nav>
</header>
